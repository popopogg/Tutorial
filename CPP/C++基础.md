# C++ 知识体系目录（不含完整项目实现，保留核心技术模块）
## 一、C++ 基础入门
1. 课程安排
2. 第一个 C 程序（HelloWorld 书写）
3. 程序注释（单行注释、多行注释）
4. 变量与常量
   - 变量的使用与意义
   - 常量与变量的区别、常量的使用
5. 关键字与标识符
   - C++ 常用编程关键字
   - 标识符命名规则
6. 数据类型
   - 基本类型（整型、实型、字符型、布尔类型）
   - 特殊类型（转义字符、字符串类型）
   - 数据输入
   - sizeof 关键字
7. 运算符
   - 算术运算符（加减乘除、取模、递增递减）
   - 赋值运算符
   - 比较运算符
   - 逻辑运算符（非、与、或）

## 二、程序流程结构
1. 选择结构
   - 单行 if 语句、多行 if 语句、多条件 if 语句
   - 嵌套 if 语句
   - 三目运算符
   - switch 语句
   - 案例（三只小猪称体重）
2. 循环结构
   - while 语句、do-while 语句、for 循环
   - 嵌套循环
   - 案例（猜数字、水仙花数、敲桌子、乘法口诀表）
3. 跳转语句
   - break 语句、continue 语句、goto 语句

## 三、数组与函数
1. 数组
   - 一维数组（定义方式、数组名、案例：五只小猪称体重、元素逆置、冒泡排序）
   - 二维数组（定义方式、数组名、案例：考试成绩统计）
2. 函数
   - 函数的定义、调用、值传递
   - 函数的常见样式、声明、分文件编写

## 四、指针与结构体
1. 指针
   - 指针的定义和使用、所占内存空间
   - 空指针、野指针、const 修饰指针
   - 指针和数组、指针和函数
   - 案例（指针配合数组和函数）
2. 结构体
   - 结构体的定义和使用、结构体数组、结构体指针
   - 结构体嵌套结构体、结构体做函数参数
   - 结构体中 const 使用场景
   - 案例（结构体案例 1、案例 2）

## 五、程序内存模型与 C++ 核心特性
1. 程序内存模型
   - 内存四区（代码区、全局区、栈区、堆区）
   - new 运算符
2. C++ 引用
   - 引用的基本语法、注意事项
   - 引用做函数参数、引用做函数返回值
   - 引用的本质、常量引用
3. 函数高级
   - 函数的默认参数、占位参数
   - 函数重载（基本语法、注意事项）

## 六、类和对象
1. 封装
   - 属性和行为作为整体、访问权限
   - C++ 中 class 和 struct 的区别
   - 成员属性设置为私有
   - 案例（立方体类、点和圆关系）
2. 对象特性
   - 构造函数和析构函数、函数分类及调用
   - 拷贝构造函数调用时机、构造函数调用规则
   - 深拷贝与浅拷贝、初始化列表
   - 类对象作为类成员、静态成员
   - 成员变量和成员函数分开存储、this 指针的用途
   - 空指针访问成员函数、const 修饰成员函数
3. 友元
   - 全局函数做友元、友元类、成员函数做友元
4. 运算符重载
   - 加号、左移、递增、赋值、关系、函数调用运算符重载
5. 继承
   - 基本语法、继承方式、继承中的对象模型
   - 构造和析构顺序、同名成员处理、同名静态成员处理
   - 多继承、菱形继承问题及解决方法
6. 多态
   - 多态的基本语法、原理剖析
   - 纯虚函数和抽象类
   - 虚析构和纯虚析构
   - 案例（计算器类、制作饮品、电脑组装）

## 七、文件操作
1. 文本文件（写文件、读文件）
2. 二进制文件（写文件、读文件）

## 八、模板与 STL
1. 模板
   - 函数模板（基本语法、注意事项、案例：数组排序）
   - 普通函数与函数模板的区别、调用规则
   - 模板的局限性
   - 类模板（基本语法、与函数模板区别、成员函数创建时机）
   - 类模板对象做函数参数、类模板与继承
   - 类模板成员函数类外实现、分文件编写、与友元
   - 案例（数组类封装）
2. STL 初识
   - STL 基本概念
   - vector 容器（存放内置数据类型、自定义数据类型、容器嵌套容器）
3. 常用 STL 容器
   - string 容器（构造、赋值、拼接、查找替换、比较、字符存取、插入删除、子串获取）
   - vector 容器（构造、赋值、容量和大小、插入删除、数据存取、互换容器、预留空间）
   - deque 容器（构造、赋值、大小操作、插入删除、数据存取、排序）
   - stack 容器（基本概念、常用接口）
   - queue 容器（基本概念、常用接口）
   - list 容器（基本概念、构造、赋值和交换、大小操作、插入删除、数据存取、反转和排序、排序案例）
   - set 容器（构造和赋值、大小和交换、插入和删除、查找和统计、set 和 multiset 区别）
   - pair 对组的创建
   - set 容器排序规则（内置类型、自定义数据类型）
   - map 容器（构造和赋值、大小和交换、插入和删除、查找和统计、排序）
4. STL 函数对象与谓词
   - 函数对象基本使用
   - 谓词（一元谓词、二元谓词）
   - 内建函数对象（算术仿函数、关系仿函数、逻辑仿函数）
5. STL 常用算法
   - 遍历算法（for_each、transform）
   - 查找算法（find、find_if、adjacent_find、binary_search、count、count_if）
   - 排序算法（sort、random_shuffle、merge、reverse）
   - 拷贝和替换算法（copy、replace、replace_if、swap）
   - 算术生成算法（accumulate、fill）
   - 集合算法（set_intersection、set_union、set_difference）
6. STL 案例
   - 评委打分、员工分组
