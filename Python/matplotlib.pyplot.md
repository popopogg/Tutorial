功能	代码
创建画布	plt.figure(figsize=(10,6))
折线图	plt.plot(x, y, 'r-o', label='名称')
散点图	plt.scatter(x, y, c=colors, s=sizes)
柱状图	plt.bar(x, height)
直方图	plt.hist(data, bins=30)
显示图片	plt.imshow(img, cmap='gray')
子图	fig, axes = plt.subplots(2, 2)
标题	plt.title("标题")
轴标签	plt.xlabel() / plt.ylabel()
图例	plt.legend()
网格	plt.grid(True)
坐标范围	plt.xlim(0,10) / plt.ylim(0,10)
隐藏坐标轴	plt.axis('off')
保存	plt.savefig('name.png', dpi=300)
显示	plt.show()
关闭	plt.close()
